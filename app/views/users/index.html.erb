
<h1>Index</h1>
 <% @users.each do |user| %> 
 <p>
 	<%= button_to "showdetails",user %>
 </p>
 	<%= button_to "destroy",user_path(user),data:{
 		turbo_method: :delete,
 		turbo_confirm: "are you sure?"
 	} %>
  <% end %>
 <p>
 	<%= button_to "add new details",new_user_path %>
 </p>

  <h1>Click the Button to Change Background Color</h1>
  <button id="change-color-button">Change Color</button>