<%= form_with(model: practice_project) do |form| %>
  <% if practice_project.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(practice_project.errors.count, "error") %> prohibited this practice_project from being saved:</h2>

      <ul>
        <% practice_project.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
